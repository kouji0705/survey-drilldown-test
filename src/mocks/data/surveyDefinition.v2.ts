import type { SurveyDefinitionV2 } from "../../types/surveyDefinition";
import sample from "./surveyDefinition.sample.json";

/** 新スキーマ（id / title / fields / options）のサンプル定義 */
export const mockSurveyDefinitionV2 = sample as SurveyDefinitionV2;
